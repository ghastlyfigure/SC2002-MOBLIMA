package Model;

public enum CinemaType implements TicketCostModifier{
	/**
	 * Enumerated type for better readability and easier referencing to attribute
	 */
	BronzeClass,
	SilverClass,
	GoldClass
}
